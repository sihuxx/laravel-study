## 🗄️ Model (모델) 

### (1) 모델이란?
**"데이터베이스 관리자"**
**파일 위치** - app/Models/user.php
**역할** - 데이터베이스 테이블과 1:1로 연결되어 데이터를 조회(Select), 저장(Insert), 수정(Update), 삭제(Delete)하는 역할을 담당.

### (2) 핵심 역할
* **데이터 구조 정의:** 어떤 데이터를 저장할지, 어떤 데이터를 숨길지 결정
* **비즈니스 로직:** 데이터와 관련된 간단한 가공 처리
* **관계 설정:** 다른 테이블(예: User와 Post)과의 연결 고리 제작

### (3) 코드 예시

```php
class Post extends Model
{
    // 1. 대량 할당 보호 ($fillable)
    // 보안을 위해 사용자 입력값으로 한 번에 채울 수 있는 컬럼만 지정합니다.
    protected $fillable = ['title', 'content', 'user_id'];

    // 2. 데이터 숨김 ($hidden)
    // API 응답 등에서 비밀번호 같은 민감한 정보를 자동으로 숨깁니다.
    protected $hidden = ['secret_code'];

    // 3. 데이터 형변환 (Casts)
    // DB에서 가져올 때 타입을 자동으로 변환해줍니다. (예: 0/1 -> boolean)
    protected function casts(): array
    {
        return [
            'is_published' => 'boolean',
        ];
    }
    
    // 4. 관계 정의 (Relationship)
    // "게시글은 한 명의 작성자(User)를 가진다"
    public function user() {
        return $this->belongsTo(User::class);
    }
}